{
  "name": "discord-patch-bot",
  "version": "1.0.0",
  "description": "Bot Discord pentru update-uri automate la jocuri",
  "main": "app/main.js",
  "scripts": {
    "start": "node app/main.js",
    "check:config": "node scripts/check-config.js",
    "check:syntax": "node scripts/check-syntax.js",
    "typecheck": "tsc --noEmit",
    "lint": "npm run check:syntax",
    "test": "node --test",
    "test:functional": "node --test test/",
    "test:regression": "node --test test/commands-regression.test.js test/configValidator.test.js",
    "audit": "npm audit --omit=dev --audit-level=moderate",
    "precommit": "npm run check && npm run audit",
    "check": "npm run check:config && npm run check:syntax && npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "1.7.7",
    "cheerio": "1.0.0-rc.12",
    "discord.js": "14.26.0",
    "mongoose": "8.2.1",
    "rss-parser": "3.13.0",
    "zod": "3.22.4"
  },
  "devDependencies": {
    "@types/node": "20.19.39",
    "typescript": "5.9.3"
  }
}
