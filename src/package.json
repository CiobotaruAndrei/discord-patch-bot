{
  "name": "discord-patch-bot",
  "version": "1.0.0",
  "description": "Bot Discord pentru update-uri automate la jocuri",
  "main": "dist/app/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/app/main.js",
    "check:config": "npm run build && node dist/scripts/check-config.js",
    "check:syntax": "npm run build && node dist/scripts/check-syntax.js",
    "typecheck": "tsc --noEmit",
    "lint": "npm run typecheck",
    "test": "npm run build && node --test dist/test",
    "test:functional": "npm run test",
    "test:regression": "npm run build && node --test dist/test/commands-regression.test.js dist/test/configValidator.test.js",
    "audit": "npm audit --omit=dev --audit-level=moderate",
    "precommit": "npm run check && npm run audit",
    "check": "npm run typecheck && npm run build && npm run check:syntax && node dist/scripts/check-config.js && node --test dist/test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "1.7.7",
    "cheerio": "1.0.0-rc.12",
    "discord.js": "14.26.0",
    "mongoose": "8.2.1",
    "rss-parser": "3.13.0",
    "zod": "3.22.4"
  },
  "devDependencies": {
    "@types/node": "20.19.39",
    "typescript": "5.9.3"
  }
}
