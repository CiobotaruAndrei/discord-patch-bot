[package]
name = "discord_patch_bot_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", default-features = false, features = ["napi4"] }
napi-derive = "2"
sha2 = "0.11"

[build-dependencies]
napi-build = "2"
