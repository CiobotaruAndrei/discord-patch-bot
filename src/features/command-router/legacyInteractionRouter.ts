"use strict";

// Compatibility adapter while the remaining /latest, /dlc, /status and autocomplete
// paths are extracted from the legacy command file into dedicated handlers.
export = require("../commands/interactions");
